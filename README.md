# MidtermRepo_ZachFalzone

# Flask Web App on GCP Description

This project is a simple Flask web application deployed on a GCP virtual machine. The app runs on port 8080 and is accessible through the VM's external IP address. It demonstrates a basic Flask server configuration and a simple To-Do list web interface.